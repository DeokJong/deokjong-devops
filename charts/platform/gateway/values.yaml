namespace: gateway-system
gatewayClassName: istio

gateways: []
# - name: ""                      (required)
#   exposure: ""                   (required, label: gateway.deokjong.io/exposure)
#   annotations: {}
#   gatewayClassName: ""           (optional, overrides default)
#   disableDefaultListeners: false (set true to skip HTTP/HTTPS listeners; e.g. TCP-only gateways)
#   certificateRef:
#     name: ""                     (required unless disableDefaultListeners is true)
#     namespace: ""                (optional, defaults to .Values.namespace)
#   allowedRoutes:
#     namespaces:
#       from: All
#   extraListeners: []             (raw Gateway API listener objects)

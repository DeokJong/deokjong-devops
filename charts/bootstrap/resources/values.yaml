nodeClasses:
  #- name: default
  #  role: ""
  #  ephemeralStorage:
  #    size: 80Gi
  #    iops: 3000
  #    throughput: 125
  #  networkPolicy: DefaultAllow
  #  networkPolicyEventLogs: Disabled
  #  snatPolicy: Random
  #  securityGroupSelectorTerms: []
  #  subnetSelectorTerms: []

nodePools:
  # - name: system
  # nodeClassName: default
  # disruption:
  #   consolidationPolicy: WhenEmptyOrUnderutilized  (default)
  #   consolidateAfter: 30s                          (default)
  #   budgets: [{nodes: "10%"}]                      (default)
  # expireAfter: 336h                                (default)
  # terminationGracePeriod: 24h0m0s                  (default)
  # requirements:                                    (default: on-demand/spot, c/m/r, gen>4, amd64/arm64, linux)
  #   - key: kubernetes.io/arch                      (same key = overwrite)
  #     operator: In
  #     values: [arm64]
  #   - key: node.kubernetes.io/instance-type        (new key = append)
  #     operator: In
  #     values: [m7g.large]
  # taints:
  #  - key: CriticalAddonsOnly
  #    effect: NoSchedule

storageClasses:
  #- name: gp3
  #  provisioner: ebs.csi.eks.amazonaws.com
  #  parameters:
  #    type: gp3
  #    fsType: ext4
  #  reclaimPolicy: Delete
  #  volumeBindingMode: WaitForFirstConsumer
